# Brooks-Lint (L0) input brief

## Claim

- Claim:
- Owner:
- Decision requested (merge / ship / accept debt / other):
- Authority path:
- Claim ceiling:

## Scope

- Paths / PR / diff:
- Mode preference (full / light / auto):

## Artifacts

- Diff or file list:
- Test commands + raw output:
- Config / permission / rollback notes:
- Other raw evidence:

## Notes for the reviewer

Apply `SKILL.md` + `references/` (including Evidence Gate and Scope Refusal).
Return JSON matching `verdict.schema.json`.
Claiming Falsify still requires `python -m falsify review` or `python -m falsify brooks`.
